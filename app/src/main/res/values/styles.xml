<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AGREGA ESTE ESTILO PADRE -->
    <style name="Widget.Carrishop" parent="">
        <!-- Puedes dejarlo vacío o añadir atributos comunes -->
    </style>

    <!-- Tu estilo existente ahora tiene un padre válido -->
    <style name="Widget.Carrishop.NivelEstrella" parent="Widget.Carrishop">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginStart">6dp</item>
        <item name="android:layout_marginEnd">6dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/texto</item>
        <item name="android:background">@drawable/bg_nivel_inactivo</item>
        <item name="android:gravity">center</item>
    </style>
      <!-- Tema base para tu aplicación (modo claro) -->
        <style name="Base.Theme.Carrishop" parent="Theme.Material3.DayNight.NoActionBar">
            <!-- Aquí puedes personalizar colores y otros atributos para el modo claro -->
            <!-- Por ejemplo: -->
            <!-- <item name="colorPrimary">@color/mi_color_primario_claro</item> -->
        </style>

        <style name="Theme.Carrishop" parent="Base.Theme.Carrishop" />
</resources>

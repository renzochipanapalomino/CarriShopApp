<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardElevation="0dp"
    app:cardUseCompatPadding="false"
    app:cardCornerRadius="16dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/blanco"
    app:cardBackgroundColor="@color/azul_claro">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/chkProcesar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Marcar"
            android:textColor="@color/error"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtNombreLista"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/texto"
                android:textSize="16sp"
                android:textStyle="bold"
                android:text="Producto"/>

            <TextView
                android:id="@+id/txtDetalleLista"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Pendiente"
                android:textColor="@color/primario_oscuro"
                android:textSize="13sp"/>
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnEliminarLista"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/bg_btn_secondary"
            android:src="@drawable/ic_delete_24"
            android:contentDescription="@string/lista_eliminar"
            app:tint="@color/exito"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
